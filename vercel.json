{
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then npm run db:migrate:check; fi && npm run build",
  "functions": {
    "src/app/api/tutor/respond/route.ts": {
      "maxDuration": 60
    }
  }
}
